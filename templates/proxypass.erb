# file managed by puppet

<Proxy <%= url %>*>
    Order deny,allow
    Allow from all
    ProxySet timeout=600 keepalive=On retry=5
</Proxy>

ProxyPassReverse <%= location %> <%= url %>

RewriteEngine On
RewriteRule <%= location %>(.*)  <%= url %>$1 [P,L]
